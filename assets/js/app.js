import "../css/app.css"

import "phoenix_html"

import { Socket } from "phoenix"

// We'll put the code within an async function, so we could use await
async function init() {
  // TODO: implement
}

init();
